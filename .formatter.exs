# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "test_helpers/test_app/{mix,.formatter}.exs",
    "test_helpers/test_app/{config,lib,test}/**/*.{ex,exs}"
  ]
]
